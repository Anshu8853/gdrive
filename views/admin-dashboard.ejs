<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <meta name="format-detection" content="telephone=no">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <title>Admin Dashboard - GDrive</title>
    <style>
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body { 
            font-family: 'Segoe UI', 'Roboto', 'Helvetica Neue', Arial, sans-serif; 
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 10px;
            overflow-x: hidden;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
        }
        .container { 
            max-width: 1200px; 
            margin: 0 auto;
            background: white; 
            padding: 40px; 
            border-radius: 20px; 
            box-shadow: 0 20px 60px rgba(0,0,0,0.15);
            position: relative;
            overflow: hidden;
        }
        .container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 5px;
            background: linear-gradient(90deg, #667eea, #764ba2);
        }
        h1 { 
            text-align: center; 
            color: #2c3e50; 
            margin-bottom: 10px;
            font-size: 32px;
            font-weight: 700;
        }
        .subtitle { 
            text-align: center; 
            color: #6c757d; 
            margin-bottom: 40px;
            font-size: 18px;
            line-height: 1.5;
        }
        .header { 
            display: flex; 
            justify-content: space-between; 
            align-items: center; 
            margin-bottom: 40px;
            padding: 20px 0;
            border-bottom: 2px solid #e9ecef;
        }
        .admin-info { 
            color: #2c3e50; 
            font-size: 20px;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .back-btn { 
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white; 
            padding: 12px 24px; 
            border: none; 
            border-radius: 10px; 
            text-decoration: none; 
            font-size: 14px;
            font-weight: 600;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        .back-btn:hover { 
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(102, 126, 234, 0.3);
            color: white;
        }
        .stats-grid { 
            display: grid; 
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); 
            gap: 25px; 
            margin-bottom: 50px;
        }
        .stat-card { 
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            padding: 30px; 
            border-radius: 15px; 
            border: 2px solid #dee2e6;
            text-align: center;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }
        .stat-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, #667eea, #764ba2);
        }
        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 35px rgba(0,0,0,0.1);
            border-color: #667eea;
        }
        .stat-number { 
            font-size: 3rem; 
            font-weight: 700; 
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 10px;
        }
        .stat-label { 
            color: #6c757d; 
            font-size: 16px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        .stat-icon {
            font-size: 48px;
            margin-bottom: 15px;
            opacity: 0.8;
        }
        .users-section { margin-bottom: 40px; }
        .users-section h2 { 
            color: #2c3e50; 
            margin-bottom: 30px;
            font-size: 24px;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 15px;
            border-bottom: 3px solid transparent;
            border-image: linear-gradient(90deg, #667eea, #764ba2) 1;
            padding-bottom: 15px;
        }
        .user-card { 
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            margin-bottom: 20px; 
            border-radius: 15px; 
            border: 2px solid #dee2e6; 
            overflow: hidden;
            transition: all 0.3s ease;
        }
        .user-card:hover {
            transform: translateY(-3px);
            box-shadow: 0 12px 30px rgba(0,0,0,0.1);
            border-color: #667eea;
        }
        .user-header { 
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white; 
            padding: 20px; 
            font-weight: 600;
            font-size: 18px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .user-body { padding: 25px; }
        .user-info { 
            display: grid; 
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); 
            gap: 20px; 
            margin-bottom: 20px;
        }
        .info-item { 
            background: white;
            padding: 15px;
            border-radius: 10px;
            border: 1px solid #e9ecef;
        }
        .info-label { 
            font-weight: 600; 
            color: #667eea; 
            font-size: 12px; 
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 8px;
        }
        .info-value { 
            color: #2c3e50; 
            font-size: 16px;
            font-weight: 500;
        }
        .files-list { 
            margin-top: 20px;
            background: white;
            border-radius: 12px;
            padding: 20px;
            border: 1px solid #e9ecef;
        }
        .files-header {
            font-weight: 600;
            color: #667eea;
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 8px;
        }
        .file-item { 
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            padding: 15px 20px; 
            margin-bottom: 10px; 
            border-radius: 10px; 
            border: 2px solid #dee2e6; 
            display: flex; 
            justify-content: space-between; 
            align-items: center;
            transition: all 0.3s ease;
        }
        .file-item:hover {
            transform: translateX(5px);
            border-color: #667eea;
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.1);
        }
        .file-info { flex: 1; }
        .file-name { 
            color: #2c3e50; 
            font-size: 14px; 
            font-weight: 500;
            margin-bottom: 5px;
        }
        .file-date {
            color: #6c757d;
            font-size: 12px;
            font-style: italic;
        }
        .file-actions { 
            display: flex; 
            gap: 10px; 
            align-items: center;
        }
        .view-btn { 
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white; 
            border: none; 
            padding: 8px 16px; 
            border-radius: 6px; 
            text-decoration: none; 
            font-size: 12px; 
            cursor: pointer;
            font-weight: 600;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        .view-btn:hover { 
            background: linear-gradient(135deg, #764ba2 0%, #667eea 100%);
            color: white;
            transform: translateY(-1px);
            box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);
        }
        .delete-btn { 
            background: linear-gradient(135deg, #ff6b6b 0%, #ee5a52 100%);
            color: white; 
            border: none; 
            padding: 8px 16px; 
            border-radius: 6px; 
            cursor: pointer; 
            font-size: 12px; 
            font-weight: 600;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        .delete-btn:hover { 
            background: linear-gradient(135deg, #ee5a52 0%, #c62828 100%);
            transform: translateY(-1px);
            box-shadow: 0 4px 12px rgba(255, 107, 107, 0.3);
        }
        .delete-btn:disabled {
            opacity: 0.5;
            cursor: not-allowed;
            transform: none;
        }
        .no-files { 
            color: #6c757d; 
            font-style: italic; 
            text-align: center; 
            padding: 30px;
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            border-radius: 10px;
            border: 2px dashed #dee2e6;
        }
        .no-files-icon {
            font-size: 48px;
            margin-bottom: 10px;
            opacity: 0.7;
        }
        .role-badge { 
            display: inline-block; 
            padding: 6px 12px; 
            border-radius: 20px; 
            font-size: 11px; 
            font-weight: bold; 
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        .role-admin { 
            background: linear-gradient(135deg, #ff6b6b 0%, #ee5a52 100%);
            color: white;
        }
        .role-user { 
            background: linear-gradient(135deg, #51cf66 0%, #40c057 100%);
            color: white;
        }
        .delete-user-btn {
            background: linear-gradient(135deg, #ff6b6b 0%, #ee5a52 100%);
            color: white;
            border: none;
            padding: 6px 10px;
            border-radius: 8px;
            cursor: pointer;
            font-size: 12px;
            font-weight: 600;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            min-width: 32px;
            min-height: 32px;
        }
        .delete-user-btn:hover {
            background: linear-gradient(135deg, #ee5a52 0%, #c62828 100%);
            transform: translateY(-1px);
            box-shadow: 0 4px 12px rgba(255, 107, 107, 0.3);
        }
        .delete-user-btn:disabled {
            opacity: 0.5;
            cursor: not-allowed;
            transform: none;
        }
        .success { 
            background: linear-gradient(135deg, #51cf66 0%, #40c057 100%);
            color: white; 
            padding: 16px 20px; 
            margin-bottom: 25px; 
            border-radius: 12px;
            font-weight: 500;
            box-shadow: 0 8px 20px rgba(81, 207, 102, 0.3);
            border-left: 5px solid #2e7d32;
        }
        .error { 
            background: linear-gradient(135deg, #ff6b6b 0%, #ee5a52 100%);
            color: white; 
            padding: 16px 20px; 
            margin-bottom: 25px; 
            border-radius: 12px;
            font-weight: 500;
            box-shadow: 0 8px 20px rgba(255, 107, 107, 0.3);
            border-left: 5px solid #c62828;
        }
        
        @media (max-width: 768px) {
            body {
                padding: 5px;
                font-size: 16px; /* Prevent zoom on iOS */
            }
            
            .container { 
                margin: 5px; 
                padding: 20px 15px;
                border-radius: 15px;
                min-height: calc(100vh - 20px);
            }
            
            h1 { 
                font-size: 22px; 
                margin-bottom: 8px;
            }
            
            .subtitle { 
                font-size: 14px; 
                margin-bottom: 25px;
                line-height: 1.4;
            }
            
            .header { 
                flex-direction: column; 
                gap: 15px; 
                text-align: center;
                padding: 15px 0;
                margin-bottom: 25px;
            }
            
            .admin-info { 
                font-size: 16px;
                line-height: 1.4;
            }
            
            .back-btn {
                width: 100%;
                padding: 12px 20px;
                font-size: 14px;
                text-align: center;
            }
            
            .stats-grid { 
                grid-template-columns: repeat(2, 1fr); 
                gap: 15px; 
                margin-bottom: 30px;
            }
            
            .stat-card { 
                padding: 20px 15px;
                text-align: center;
            }
            
            .stat-number { 
                font-size: 2rem;
                margin-bottom: 8px;
            }
            
            .stat-label {
                font-size: 12px;
                line-height: 1.3;
            }
            
            .stat-icon {
                font-size: 32px;
                margin-bottom: 10px;
            }
            
            .users-section h2 { 
                font-size: 18px;
                text-align: center;
                margin-bottom: 20px;
            }
            
            .user-card {
                margin-bottom: 15px;
                border-radius: 12px;
            }
            
            .user-header {
                padding: 15px;
                font-size: 16px;
                flex-direction: column;
                gap: 8px;
                text-align: center;
            }
            
            .delete-user-btn {
                padding: 8px 10px;
                font-size: 14px;
                min-width: 36px;
                min-height: 36px;
            }
            
            .user-body {
                padding: 20px 15px;
            }
            
            .user-info { 
                grid-template-columns: 1fr; 
                gap: 12px;
                margin-bottom: 15px;
            }
            
            .info-item {
                padding: 12px;
                border-radius: 8px;
            }
            
            .info-label {
                font-size: 11px;
                margin-bottom: 6px;
            }
            
            .info-value {
                font-size: 14px;
                line-height: 1.3;
            }
            
            .files-list {
                padding: 15px;
                border-radius: 10px;
            }
            
            .files-header {
                font-size: 12px;
                margin-bottom: 12px;
            }
            
            .file-item { 
                flex-direction: column; 
                align-items: flex-start; 
                gap: 12px;
                padding: 15px;
                margin-bottom: 8px;
            }
            
            .file-info { 
                width: 100%;
            }
            
            .file-name {
                font-size: 13px;
                word-break: break-all;
                margin-bottom: 6px;
            }
            
            .file-date {
                font-size: 11px;
            }
            
            .file-actions { 
                width: 100%; 
                justify-content: center;
                gap: 8px;
            }
            
            .view-btn, .delete-btn {
                flex: 1;
                padding: 10px 12px;
                font-size: 12px;
                min-height: 40px;
                text-align: center;
            }
            
            .no-files {
                padding: 25px 15px;
                font-size: 13px;
            }
            
            .no-files-icon {
                font-size: 36px;
                margin-bottom: 8px;
            }
            
            .success, .error {
                padding: 12px 15px;
                margin-bottom: 20px;
                font-size: 14px;
                border-radius: 10px;
            }
        }
        
        @media (max-width: 480px) {
            body {
                padding: 2px;
            }
            
            .container { 
                margin: 2px; 
                padding: 15px 10px;
                border-radius: 10px;
            }
            
            h1 { 
                font-size: 18px;
                margin-bottom: 6px;
            }
            
            .subtitle { 
                font-size: 12px;
                margin-bottom: 20px;
            }
            
            .header {
                gap: 12px;
                padding: 12px 0;
                margin-bottom: 20px;
            }
            
            .admin-info {
                font-size: 14px;
            }
            
            .back-btn { 
                padding: 10px 15px; 
                font-size: 12px;
                width: 100%;
            }
            
            .stats-grid {
                grid-template-columns: 1fr;
                gap: 12px;
                margin-bottom: 25px;
            }
            
            .stat-card {
                padding: 15px 10px;
            }
            
            .stat-number { 
                font-size: 1.8rem;
                margin-bottom: 6px;
            }
            
            .stat-label { 
                font-size: 11px;
            }
            
            .stat-icon {
                font-size: 28px;
                margin-bottom: 8px;
            }
            
            .users-section h2 {
                font-size: 16px;
                margin-bottom: 15px;
            }
            
            .user-header { 
                padding: 12px; 
                font-size: 14px;
                gap: 6px;
            }
            
            .role-badge {
                padding: 4px 8px;
                font-size: 10px;
            }
            
            .user-body { 
                padding: 15px 10px;
            }
            
            .user-info {
                gap: 10px;
                margin-bottom: 12px;
            }
            
            .info-item {
                padding: 10px;
                border-radius: 6px;
            }
            
            .info-label {
                font-size: 10px;
                margin-bottom: 4px;
            }
            
            .info-value {
                font-size: 12px;
            }
            
            .files-list {
                padding: 12px;
                border-radius: 8px;
            }
            
            .files-header {
                font-size: 11px;
                margin-bottom: 10px;
            }
            
            .file-item {
                padding: 12px;
                margin-bottom: 6px;
                gap: 10px;
            }
            
            .file-name {
                font-size: 12px;
                margin-bottom: 4px;
            }
            
            .file-date {
                font-size: 10px;
            }
            
            .file-size {
                font-size: 10px;
            }
            
            .view-btn, .delete-btn { 
                padding: 8px 10px; 
                font-size: 11px;
                min-width: 50px;
                min-height: 36px;
            }
            
            .no-files {
                padding: 20px 10px;
                font-size: 12px;
            }
            
            .no-files-icon {
                font-size: 30px;
                margin-bottom: 6px;
            }
        }
        
        /* Touch-friendly improvements for Android */
        @media (pointer: coarse) {
            .view-btn, .delete-btn, .back-btn, .delete-user-btn {
                min-height: 44px; /* Apple's recommended touch target size */
                min-width: 44px;
                -webkit-tap-highlight-color: rgba(102, 126, 234, 0.2);
            }
            
            .delete-user-btn:hover {
                transform: none; /* Disable hover effects on touch devices */
            }
            
            .stat-card, .user-card, .file-item {
                -webkit-tap-highlight-color: transparent;
            }
            
            .stat-card:hover, .user-card:hover, .file-item:hover {
                transform: none; /* Disable hover effects on touch devices */
            }
            
            .user-header {
                -webkit-tap-highlight-color: transparent;
            }
            
            /* Larger touch targets for better usability */
            .role-badge {
                min-height: 24px;
                display: inline-flex;
                align-items: center;
                justify-content: center;
            }
        }
        
        /* Android Chrome specific optimizations */
        @media screen and (-webkit-min-device-pixel-ratio: 1) {
            .container {
                -webkit-overflow-scrolling: touch; /* Smooth scrolling */
            }
            
            /* Prevent zoom on form elements */
            input, select, textarea {
                font-size: 16px;
            }
            
            /* Better text rendering on Android */
            body {
                text-rendering: optimizeLegibility;
                -webkit-text-size-adjust: 100%;
            }
        }
        
        /* High DPI screens (Retina, Android high-density) */
        @media (-webkit-min-device-pixel-ratio: 2), (min-resolution: 192dpi) {
            .container::before {
                height: 3px; /* Thinner gradient line on high DPI */
            }
            
            .stat-icon, .no-files-icon {
                /* Ensure emojis render well on high DPI */
                -webkit-font-smoothing: antialiased;
                -moz-osx-font-smoothing: grayscale;
            }
        }
        
        .loading {
            opacity: 0.7;
            pointer-events: none;
        }
        
        .fade-in {
            animation: fadeIn 0.6s ease-out;
        }
        
        @keyframes fadeIn {
            from { 
                opacity: 0; 
                transform: translateY(20px); 
            }
            to { 
                opacity: 1; 
                transform: translateY(0); 
            }
        }
        
        .pulse {
            animation: pulse 2s infinite;
        }
        
        @keyframes pulse {
            0% { 
                transform: scale(1); 
            }
            50% { 
                transform: scale(1.05); 
            }
            100% { 
                transform: scale(1); 
            }
        }
        
        .admin-icon {
            font-size: 24px;
            margin-right: 10px;
            background: linear-gradient(135deg, #ffd700 0%, #ffb700 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        
        .file-size {
            color: #6c757d;
            font-size: 11px;
            margin-left: 8px;
            font-weight: 500;
        }
        
        .status-online {
            display: inline-block;
            width: 8px;
            height: 8px;
            background: #51cf66;
            border-radius: 50%;
            margin-left: 8px;
            animation: pulse 2s infinite;
        }
        
        .status-offline {
            display: inline-block;
            width: 8px;
            height: 8px;
            background: #6c757d;
            border-radius: 50%;
            margin-left: 8px;
        }
        
        .tooltip {
            position: relative;
            cursor: help;
        }
        
        .tooltip::after {
            content: attr(data-tooltip);
            position: absolute;
            bottom: 120%;
            left: 50%;
            transform: translateX(-50%);
            background: #2c3e50;
            color: white;
            padding: 8px 12px;
            border-radius: 6px;
            font-size: 12px;
            white-space: nowrap;
            opacity: 0;
            visibility: hidden;
            transition: all 0.3s ease;
            z-index: 1000;
        }
        
        .tooltip:hover::after {
            opacity: 1;
            visibility: visible;
        }
    </style>
</head>

<body>
    <div class="container fade-in">
        <h1>🛡️ Admin Dashboard</h1>
        <p class="subtitle">Comprehensive overview and management of all users and system resources</p>
        
        <div class="header">
            <div class="admin-info">
                <span class="admin-icon">👑</span>
                Welcome, Admin
                <span class="status-online"></span>
            </div>
            <a href="/home" class="back-btn">← Back to Home</a>
        </div>
        
        <% if (success) { %>
            <div class="success fade-in">✅ <%= success %></div>
        <% } %>
        <% if (error) { %>
            <div class="error fade-in">❌ <%= error %></div>
        <% } %>

        <div class="stats-grid">
            <div class="stat-card pulse">
                <div class="stat-icon">👥</div>
                <div class="stat-number"><%= users.length %></div>
                <div class="stat-label">Total Users</div>
            </div>
            <div class="stat-card">
                <div class="stat-icon">📁</div>
                <div class="stat-number"><%= users.reduce((sum, user) => sum + (user.files ? user.files.length : 0), 0) %></div>
                <div class="stat-label">Total Files</div>
            </div>
            <div class="stat-card">
                <div class="stat-icon">🛡️</div>
                <div class="stat-number"><%= users.filter(user => user.role === 'admin').length %></div>
                <div class="stat-label">Admins</div>
            </div>
            <div class="stat-card">
                <div class="stat-icon">📊</div>
                <div class="stat-number"><%= users.filter(user => user.role === 'user').length %></div>
                <div class="stat-label">Regular Users</div>
            </div>
        </div>

        <div class="users-section">
            <h2>👤 User Management Dashboard</h2>
            
            <% users.forEach(user => { %>
                <div class="user-card fade-in">
                    <div class="user-header">
                        <span>
                            <% if (user.role === 'admin') { %>🛡️<% } else { %>👤<% } %> 
                            <%= user.username %>
                        </span>
                        <div style="display: flex; align-items: center; gap: 10px;">
                            <span class="role-badge role-<%= user.role %>"><%= user.role %></span>
                            <% if (user.role !== 'admin') { %>
                                <button class="delete-user-btn" onclick="deleteUser('<%= user._id %>', '<%= user.username %>')" 
                                        title="Delete User">
                                    🗑️
                                </button>
                            <% } %>
                        </div>
                    </div>
                    <div class="user-body">
                        <div class="user-info">
                            <div class="info-item">
                                <div class="info-label">📧 Email Address</div>
                                <div class="info-value"><%= user.email %></div>
                            </div>
                            <div class="info-item">
                                <div class="info-label">📅 Registration Date</div>
                                <div class="info-value">
                                    <%= user.createdAt ? user.createdAt.toLocaleDateString('en-US', { 
                                        year: 'numeric', 
                                        month: 'long', 
                                        day: 'numeric',
                                        hour: '2-digit',
                                        minute: '2-digit'
                                    }) : 'Not available' %>
                                </div>
                            </div>
                            <div class="info-item">
                                <div class="info-label">📂 File Count</div>
                                <div class="info-value">
                                    <%= user.files ? user.files.length : 0 %> files
                                    <% if (user.files && user.files.length > 0) { %>
                                        <span class="status-online tooltip" data-tooltip="Active user with files"></span>
                                    <% } else { %>
                                        <span class="status-offline tooltip" data-tooltip="No files uploaded"></span>
                                    <% } %>
                                </div>
                            </div>
                            <div class="info-item">
                                <div class="info-label">🏷️ User ID</div>
                                <div class="info-value">#<%= user._id.toString().substring(0, 8) %>...</div>
                            </div>
                        </div>

                        <% if (user.files && user.files.length > 0) { %>
                            <div class="files-list">
                                <div class="files-header">
                                    📁 User Files (<%= user.files.length %>)
                                </div>
                                <% user.files.forEach(file => { %>
                                    <div class="file-item">
                                        <div class="file-info">
                                            <div class="file-name">
                                                <% 
                                                let displayName = '';
                                                let fileExtension = '';
                                                let fileIcon = '';
                                                
                                                if (typeof file === 'string') {
                                                    displayName = file;
                                                    fileIcon = '📄';
                                                } else if (file.originalName) {
                                                    displayName = file.originalName;
                                                    fileExtension = file.originalName.split('.').pop().toLowerCase();
                                                } else if (file.originalname) {
                                                    displayName = file.originalname;
                                                    fileExtension = file.originalname.split('.').pop().toLowerCase();
                                                } else {
                                                    displayName = 'Unknown file';
                                                    fileIcon = '📄';
                                                }
                                                
                                                // Set appropriate icon based on file extension
                                                if (['jpg', 'jpeg', 'png', 'gif', 'webp', 'bmp', 'tiff', 'svg'].includes(fileExtension)) {
                                                    fileIcon = '🖼️';
                                                } else if (['mp4', 'avi', 'mov', 'wmv', 'flv', 'webm', 'mkv', '3gp'].includes(fileExtension)) {
                                                    fileIcon = '🎬';
                                                } else if (['mp3', 'wav', 'aac', 'ogg', 'flac', 'm4a'].includes(fileExtension)) {
                                                    fileIcon = '🎵';
                                                } else if (['pdf'].includes(fileExtension)) {
                                                    fileIcon = '📄';
                                                } else if (['doc', 'docx'].includes(fileExtension)) {
                                                    fileIcon = '📝';
                                                } else if (['xls', 'xlsx'].includes(fileExtension)) {
                                                    fileIcon = '📊';
                                                } else if (['ppt', 'pptx'].includes(fileExtension)) {
                                                    fileIcon = '📽️';
                                                } else if (['zip', 'rar', '7z', 'tar', 'gz'].includes(fileExtension)) {
                                                    fileIcon = '🗜️';
                                                } else if (['txt', 'rtf'].includes(fileExtension)) {
                                                    fileIcon = '📃';
                                                } else if (['js', 'css', 'html', 'json', 'xml'].includes(fileExtension)) {
                                                    fileIcon = '💻';
                                                } else if (!fileIcon) {
                                                    fileIcon = '📄';
                                                }
                                                %>
                                                <%= fileIcon %> <%= displayName %>
                                                <span class="file-size tooltip" data-tooltip="File size">
                                                    (<%= (file.size / 1024).toFixed(1) %> KB)
                                                </span>
                                            </div>
                                            <div class="file-date">
                                                🕒 Uploaded: <%= file.uploadDate ? file.uploadDate.toLocaleDateString('en-US', {
                                                    year: 'numeric',
                                                    month: 'short', 
                                                    day: 'numeric',
                                                    hour: '2-digit',
                                                    minute: '2-digit'
                                                }) : 'Unknown date' %>
                                            </div>
                                        </div>
                                        <div class="file-actions">
                                            <%
                                                // Generate Cloudinary URL
                                                let fileUrl = '';
                                                if (typeof file === 'string') {
                                                    // Old format - string filename
                                                    fileUrl = `https://res.cloudinary.com/${cloudinaryCloudName}/raw/upload/${file}`;
                                                } else if (file.cloudinaryUrl) {
                                                    // New format - file has cloudinaryUrl
                                                    fileUrl = file.cloudinaryUrl;
                                                } else if (file.filename) {
                                                    // New format without cloudinaryUrl - construct URL based on file type
                                                    const originalName = file.originalName || file.originalname || '';
                                                    const extension = originalName.split('.').pop().toLowerCase();
                                                    let resourceType;
                                                    
                                                    if (['jpg', 'jpeg', 'png', 'gif', 'webp', 'bmp', 'tiff', 'svg'].includes(extension)) {
                                                        resourceType = 'image';
                                                    } else if (['mp4', 'avi', 'mov', 'wmv', 'flv', 'webm', 'mkv', '3gp'].includes(extension)) {
                                                        resourceType = 'video';
                                                    } else {
                                                        // For PDFs and other documents, use raw resource type
                                                        resourceType = 'raw';
                                                    }
                                                    
                                                    fileUrl = `https://res.cloudinary.com/${cloudinaryCloudName}/${resourceType}/upload/${file.filename}`;
                                                } else if (file.publicId) {
                                                    // Use publicId - determine resource type from original name
                                                    const originalName = file.originalName || file.originalname || '';
                                                    const extension = originalName.split('.').pop().toLowerCase();
                                                    let resourceType;
                                                    
                                                    if (['jpg', 'jpeg', 'png', 'gif', 'webp', 'bmp', 'tiff', 'svg'].includes(extension)) {
                                                        resourceType = 'image';
                                                    } else if (['mp4', 'avi', 'mov', 'wmv', 'flv', 'webm', 'mkv', '3gp'].includes(extension)) {
                                                        resourceType = 'video';
                                                    } else {
                                                        // For PDFs and other documents, use raw resource type
                                                        resourceType = 'raw';
                                                    }
                                                    
                                                    fileUrl = `https://res.cloudinary.com/${cloudinaryCloudName}/${resourceType}/upload/${file.publicId}`;
                                                    fileUrl = `https://res.cloudinary.com/${cloudinaryCloudName}/${resourceType}/upload/${file.publicId}`;
                                                }
                                                
                                                // Get file identifier for deletion
                                                let fileId = '';
                                                if (typeof file === 'string') {
                                                    fileId = file;
                                                } else if (file.filename) {
                                                    fileId = file.filename;
                                                } else if (file.publicId) {
                                                    fileId = file.publicId;
                                                }
                                            %>
                                            <% 
                                            // Special handling for different file types
                                            const originalName = file.originalName || file.originalname || '';
                                            const extension = originalName.split('.').pop().toLowerCase();
                                            const isPdf = extension === 'pdf';
                                            const isImage = ['jpg', 'jpeg', 'png', 'gif', 'webp', 'bmp', 'tiff', 'svg'].includes(extension);
                                            const isVideo = ['mp4', 'avi', 'mov', 'wmv', 'flv', 'webm', 'mkv', '3gp'].includes(extension);
                                            
                                            // Use our authenticated admin file serving route
                                            let viewUrl = `/admin/file/${user._id}/${fileId}`;
                                            let downloadUrl = viewUrl;
                                            %>
                                            
                                            <% if (isPdf) { %>
                                                <a href="<%= viewUrl %>" class="view-btn" target="_blank" style="margin-bottom: 5px;">
                                                    📖 View PDF
                                                </a>
                                                <a href="<%= downloadUrl %>" class="view-btn" target="_blank" download
                                                   style="background: linear-gradient(135deg, #28a745 0%, #20c997 100%);">
                                                    📥 Download
                                                </a>
                                            <% } else { %>
                                                <a href="<%= viewUrl %>" class="view-btn" target="_blank">
                                                    👁️ View
                                                </a>
                                            <% } %>
                                            <button class="delete-btn" onclick="deleteFile('<%= fileId %>')">
                                                🗑️ Delete
                                            </button>
                                        </div>
                                    </div>
                                <% }) %>
                            </div>
                        <% } else { %>
                            <div class="no-files">
                                <div class="no-files-icon">📂</div>
                                <div>No files uploaded by this user yet</div>
                            </div>
                        <% } %>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>

    <script>
        function deleteFile(filename) {
            if (confirm('🗑️ Are you sure you want to delete this file? This action cannot be undone.')) {
                const button = event.target;
                const originalText = button.innerHTML;
                
                button.innerHTML = '⏳ Deleting...';
                button.disabled = true;
                button.style.opacity = '0.7';
                
                fetch('/admin/delete-file/' + encodeURIComponent(filename), {
                    method: 'DELETE',
                    headers: {
                        'Content-Type': 'application/json',
                    }
                })
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        // Add success animation
                        button.innerHTML = '✅ Deleted';
                        button.style.background = 'linear-gradient(135deg, #51cf66 0%, #40c057 100%)';
                        
                        // Remove the file item with animation
                        const fileItem = button.closest('.file-item');
                        fileItem.style.transition = 'all 0.5s ease';
                        fileItem.style.transform = 'translateX(100%)';
                        fileItem.style.opacity = '0';
                        
                        setTimeout(() => {
                            window.location.reload();
                        }, 500);
                    } else {
                        button.innerHTML = '❌ Failed';
                        button.style.background = 'linear-gradient(135deg, #ff6b6b 0%, #ee5a52 100%)';
                        setTimeout(() => {
                            button.innerHTML = originalText;
                            button.disabled = false;
                            button.style.opacity = '1';
                        }, 2000);
                        alert('❌ Error: ' + (data.error || 'Failed to delete file'));
                    }
                })
                .catch(error => {
                    console.error('Delete error:', error);
                    button.innerHTML = '❌ Error';
                    button.style.background = 'linear-gradient(135deg, #ff6b6b 0%, #ee5a52 100%)';
                    setTimeout(() => {
                        button.innerHTML = originalText;
                        button.disabled = false;
                        button.style.opacity = '1';
                    }, 2000);
                    alert('❌ Network error occurred while deleting file');
                });
            }
        }
        
        function deleteUser(userId, username) {
            if (confirm(`🗑️ Are you sure you want to delete user "${username}"? This will permanently delete:\n\n• The user account\n• All their uploaded files\n• This action cannot be undone!`)) {
                const button = event.target;
                const originalText = button.innerHTML;
                
                button.innerHTML = '⏳';
                button.disabled = true;
                button.style.opacity = '0.7';
                
                fetch('/admin/delete-user/' + encodeURIComponent(userId), {
                    method: 'DELETE',
                    headers: {
                        'Content-Type': 'application/json',
                    }
                })
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        // Add success animation
                        button.innerHTML = '✅';
                        button.style.background = 'linear-gradient(135deg, #51cf66 0%, #40c057 100%)';
                        
                        // Remove the user card with animation
                        const userCard = button.closest('.user-card');
                        userCard.style.transition = 'all 0.5s ease';
                        userCard.style.transform = 'translateX(-100%)';
                        userCard.style.opacity = '0';
                        
                        // Show success message and reload
                        setTimeout(() => {
                            alert(`✅ User "${username}" has been successfully deleted.`);
                            window.location.reload();
                        }, 500);
                    } else {
                        button.innerHTML = '❌';
                        button.style.background = 'linear-gradient(135deg, #ff6b6b 0%, #ee5a52 100%)';
                        setTimeout(() => {
                            button.innerHTML = originalText;
                            button.disabled = false;
                            button.style.opacity = '1';
                        }, 2000);
                        alert('❌ Error: ' + (data.error || 'Failed to delete user'));
                    }
                })
                .catch(error => {
                    console.error('Delete user error:', error);
                    button.innerHTML = '❌';
                    button.style.background = 'linear-gradient(135deg, #ff6b6b 0%, #ee5a52 100%)';
                    setTimeout(() => {
                        button.innerHTML = originalText;
                        button.disabled = false;
                        button.style.opacity = '1';
                    }, 2000);
                    alert('❌ Network error occurred while deleting user');
                });
            }
        }
        
        // Add entrance animations
        document.addEventListener('DOMContentLoaded', function() {
            const cards = document.querySelectorAll('.user-card, .stat-card');
            cards.forEach((card, index) => {
                card.style.opacity = '0';
                card.style.transform = 'translateY(20px)';
                setTimeout(() => {
                    card.style.transition = 'all 0.6s ease';
                    card.style.opacity = '1';
                    card.style.transform = 'translateY(0)';
                }, index * 100);
            });
            
            // Add hover effects for stat cards
            const statCards = document.querySelectorAll('.stat-card');
            statCards.forEach(card => {
                card.addEventListener('mouseenter', function() {
                    this.style.transform = 'translateY(-8px) scale(1.02)';
                });
                card.addEventListener('mouseleave', function() {
                    this.style.transform = 'translateY(0) scale(1)';
                });
            });
        });
        
        // Add loading states for better UX
        document.querySelectorAll('.view-btn').forEach(btn => {
            btn.addEventListener('click', function() {
                this.innerHTML = '⏳ Loading...';
                this.style.opacity = '0.8';
            });
        });
        
        // Add smooth scrolling for better navigation
        document.documentElement.style.scrollBehavior = 'smooth';
        
        // Enhanced file size formatting
        function formatFileSize(bytes) {
            if (bytes === 0) return '0 Bytes';
            const k = 1024;
            const sizes = ['Bytes', 'KB', 'MB', 'GB'];
            const i = Math.floor(Math.log(bytes) / Math.log(k));
            return parseFloat((bytes / Math.pow(k, i)).toFixed(2)) + ' ' + sizes[i];
        }
    </script>
</body>
</html>