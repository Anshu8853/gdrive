<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <meta name="format-detection" content="telephone=no">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <title>GDrive - Welcome</title>
    <style>
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body { 
            font-family: 'Segoe UI', 'Roboto', 'Helvetica Neue', Arial, sans-serif; 
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 10px;
            overflow-x: hidden;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
        }
        .container { 
            max-width: 900px; 
            margin: 0 auto;
            background: white; 
            padding: 40px; 
            border-radius: 20px; 
            box-shadow: 0 20px 60px rgba(0,0,0,0.15);
            position: relative;
            overflow: hidden;
        }
        .container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 5px;
            background: linear-gradient(90deg, #667eea, #764ba2);
        }
        .header { 
            display: flex; 
            justify-content: space-between; 
            align-items: center; 
            margin-bottom: 30px;
            padding: 20px 0;
            border-bottom: 2px solid #e9ecef;
        }
        .user-greeting { 
            color: #2c3e50; 
            font-size: 18px;
            font-weight: 600;
        }
        .admin-link { 
            color: #667eea; 
            text-decoration: none; 
            font-size: 14px;
            padding: 8px 16px;
            background: linear-gradient(135deg, #e3f2fd 0%, #bbdefb 100%);
            border-radius: 8px;
            margin-top: 8px;
            display: inline-block;
            transition: all 0.3s ease;
            border: 1px solid #2196f3;
        }
        .admin-link:hover { 
            background: linear-gradient(135deg, #2196f3 0%, #1976d2 100%);
            color: white;
            transform: translateY(-1px);
        }
        .logout-btn { 
            background: linear-gradient(135deg, #ff6b6b 0%, #ee5a52 100%);
            color: white;
            padding: 12px 24px;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            font-size: 14px;
            font-weight: 600;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        .logout-btn:hover { 
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(255, 107, 107, 0.3);
        }
        h1 { 
            text-align: center; 
            color: #2c3e50; 
            margin-bottom: 10px;
            font-size: 32px;
            font-weight: 700;
        }
        .welcome-text { 
            text-align: center; 
            color: #6c757d; 
            margin-bottom: 40px;
            font-size: 18px;
            line-height: 1.5;
        }
        .form-group { margin-bottom: 25px; }
        label { 
            display: block; 
            margin-bottom: 12px; 
            color: #555; 
            font-weight: 600;
            font-size: 16px;
        }
        input[type=file] { 
            width: 100%; 
            padding: 20px; 
            border: 3px dashed #667eea; 
            border-radius: 15px; 
            background: linear-gradient(135deg, #f8f9ff 0%, #e6f3ff 100%);
            text-align: center;
            cursor: pointer;
            transition: all 0.3s ease;
            font-size: 16px;
            color: #667eea;
            font-weight: 500;
        }
        input[type=file]:hover { 
            background: linear-gradient(135deg, #e6f3ff 0%, #d1edff 100%);
            border-color: #764ba2;
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(102, 126, 234, 0.2);
        }
        .file-hint {
            background: linear-gradient(135deg, #e3f2fd 0%, #bbdefb 100%);
            padding: 12px 16px;
            border-radius: 10px;
            color: #1565c0;
            font-size: 14px;
            margin-top: 8px;
            border: 1px solid #2196f3;
            text-align: center;
        }
        button { 
            width: 100%; 
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white; 
            padding: 18px; 
            border: none; 
            border-radius: 12px; 
            cursor: pointer; 
            font-size: 18px;
            font-weight: 600;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 20px;
        }
        button:hover { 
            transform: translateY(-2px);
            box-shadow: 0 15px 35px rgba(102, 126, 234, 0.3);
        }
        button:active { transform: translateY(0); }
        .success { 
            background: linear-gradient(135deg, #51cf66 0%, #40c057 100%);
            color: white; 
            padding: 16px 20px; 
            margin-bottom: 25px; 
            border-radius: 12px;
            font-weight: 500;
            box-shadow: 0 8px 20px rgba(81, 207, 102, 0.3);
            border-left: 5px solid #2e7d32;
        }
        .error { 
            background: linear-gradient(135deg, #ff6b6b 0%, #ee5a52 100%);
            color: white; 
            padding: 16px 20px; 
            margin-bottom: 25px; 
            border-radius: 12px;
            font-weight: 500;
            box-shadow: 0 8px 20px rgba(255, 107, 107, 0.3);
            border-left: 5px solid #c62828;
        }
        .file-section { 
            margin-top: 50px; 
            padding-top: 40px; 
            border-top: 2px solid #e9ecef;
        }
        .file-section h2 { 
            color: #2c3e50; 
            margin-bottom: 25px;
            font-size: 24px;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .file-count {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 6px 12px;
            border-radius: 20px;
            font-size: 14px;
            font-weight: 600;
        }
        .file-item { 
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            padding: 20px; 
            margin-bottom: 15px; 
            border-radius: 15px; 
            border: 2px solid #dee2e6; 
            display: flex; 
            justify-content: space-between; 
            align-items: center;
            transition: all 0.3s ease;
        }
        .file-item:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.1);
            border-color: #667eea;
        }
        .file-info { flex: 1; }
        .file-name { 
            color: #2c3e50; 
            font-weight: 600; 
            margin-bottom: 6px;
            font-size: 16px;
            word-break: break-word;
        }
        .file-date { 
            color: #6c757d; 
            font-size: 13px;
            font-style: italic;
        }
        .file-actions { 
            display: flex; 
            gap: 12px; 
            align-items: center;
        }
        .view-btn { 
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white; 
            border: none; 
            padding: 10px 20px; 
            border-radius: 8px; 
            text-decoration: none; 
            font-size: 14px; 
            cursor: pointer;
            font-weight: 600;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        .view-btn:hover { 
            background: linear-gradient(135deg, #764ba2 0%, #667eea 100%);
            color: white;
            transform: translateY(-1px);
            box-shadow: 0 6px 15px rgba(102, 126, 234, 0.3);
        }
        .delete-btn { 
            background: linear-gradient(135deg, #ff6b6b 0%, #ee5a52 100%);
            color: white; 
            border: none; 
            padding: 10px 20px; 
            border-radius: 8px; 
            cursor: pointer; 
            font-size: 14px; 
            font-weight: 600;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        .delete-btn:hover { 
            background: linear-gradient(135deg, #ee5a52 0%, #c62828 100%);
            transform: translateY(-1px);
            box-shadow: 0 6px 15px rgba(255, 107, 107, 0.3);
        }
        .no-files { 
            text-align: center; 
            color: #6c757d; 
            padding: 40px; 
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            border-radius: 15px;
            border: 2px dashed #dee2e6;
        }
        .no-files-icon {
            font-size: 48px;
            margin-bottom: 15px;
            opacity: 0.7;
        }
        .upload-section {
            background: linear-gradient(135deg, #e3f2fd 0%, #bbdefb 100%);
            padding: 30px;
            border-radius: 15px;
            margin-bottom: 40px;
            border: 2px solid #2196f3;
        }
        .upload-icon {
            text-align: center;
            font-size: 48px;
            margin-bottom: 20px;
            color: #667eea;
        }
        
        /* File Type Specific Styling */
        .file-name {
            display: flex;
            align-items: center;
            gap: 8px;
            font-weight: 600;
            word-break: break-word;
        }
        .file-actions {
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
        }
        .view-btn {
            flex: 1;
            min-width: 120px;
            text-align: center;
        }
        .delete-btn {
            flex: 1;
            min-width: 120px;
        }
        
        /* Tooltip for file extensions */
        .file-extension-tooltip {
            position: relative;
            cursor: help;
        }
        .file-extension-tooltip:hover::after {
            content: attr(data-type);
            position: absolute;
            bottom: 100%;
            left: 50%;
            transform: translateX(-50%);
            background: rgba(0,0,0,0.8);
            color: white;
            padding: 5px 10px;
            border-radius: 4px;
            font-size: 12px;
            white-space: nowrap;
            z-index: 1000;
        }
        
        /* Android & Mobile Optimizations */
        @media (max-width: 768px) {
            body {
                padding: 5px;
                font-size: 16px; /* Prevent zoom on iOS */
            }
            
            .container { 
                margin: 5px;
                padding: 20px 15px;
                border-radius: 15px;
                min-height: calc(100vh - 20px);
            }
            
            .header { 
                flex-direction: column;
                gap: 15px;
                text-align: center;
                padding: 15px 0;
                margin-bottom: 25px;
            }
            
            .user-greeting { 
                font-size: 16px;
                line-height: 1.4;
            }
            
            .admin-link {
                font-size: 13px;
                padding: 10px 15px;
                margin-top: 8px;
                display: inline-block;
            }
            
            .logout-btn { 
                width: 100%;
                padding: 12px;
                font-size: 14px;
                margin-top: 10px;
            }
            
            .upload-section { 
                padding: 25px 15px;
                margin-bottom: 30px;
            }
            
            .upload-section h2 { 
                font-size: 20px;
                margin-bottom: 20px;
                text-align: center;
            }
            
            .form-group { 
                margin-bottom: 20px;
            }
            
            label { 
                font-size: 14px;
                padding: 15px 20px;
                margin-bottom: 15px;
                text-align: center;
                min-height: 60px;
                display: flex;
                align-items: center;
                justify-content: center;
            }
            
            button { 
                width: 100%;
                padding: 15px;
                font-size: 16px;
                margin-top: 15px;
                border-radius: 12px;
            }
            
            .file-section { 
                margin-top: 30px;
                padding-top: 25px;
            }
            
            .file-section h2 { 
                font-size: 18px;
                text-align: center;
                flex-direction: column;
                gap: 8px;
            }
            
            .file-count {
                font-size: 12px;
                padding: 4px 10px;
            }
            
            .file-grid { 
                grid-template-columns: 1fr;
                gap: 15px;
            }
            
            .file-card { 
                padding: 20px 15px;
            }
            
            .file-name { 
                font-size: 14px;
                word-break: break-all;
                margin-bottom: 8px;
            }
            
            .file-actions { 
                flex-direction: column;
                gap: 10px;
                margin-top: 15px;
            }
            
            .file-actions a,
            .file-actions button { 
                width: 100%;
                padding: 12px;
                font-size: 14px;
                text-align: center;
            }
            
            .no-files { 
                padding: 30px 15px;
                font-size: 14px;
            }
            
            .success, .error { 
                padding: 15px;
                margin-bottom: 20px;
                font-size: 14px;
                border-radius: 8px;
            }
            
            .upload-icon {
                font-size: 36px;
                margin-bottom: 15px;
            }
        }
        
        @media (max-width: 480px) {
            .container {
                margin: 2px;
                padding: 15px 10px;
                border-radius: 10px;
            }
            
            .user-greeting {
                font-size: 14px;
            }
            
            .upload-section h2 {
                font-size: 18px;
            }
            
            label {
                padding: 12px 15px;
                font-size: 13px;
                min-height: 50px;
            }
            
            button {
                padding: 12px;
                font-size: 14px;
            }
            
            .file-card {
                padding: 15px 10px;
            }
            
            .file-name {
                font-size: 13px;
            }
            
            .file-actions a,
            .file-actions button {
                padding: 10px;
                font-size: 13px;
            }
        }
        
        /* Touch-friendly improvements */
        @media (pointer: coarse) {
            button, .file-actions a, .file-actions button {
                min-height: 44px; /* Apple's recommended touch target size */
                min-width: 44px;
            }
            
            label[for="file"] {
                min-height: 60px;
                cursor: pointer;
                -webkit-tap-highlight-color: rgba(102, 126, 234, 0.2);
            }
            
            .file-card:hover {
                transform: none; /* Disable hover effects on touch devices */
            }
            
            button:hover {
                transform: none;
            }
        }
        
        /* Android Chrome specific optimizations */
        @media screen and (-webkit-min-device-pixel-ratio: 1) {
            input[type="file"] {
                font-size: 16px; /* Prevent zoom on focus */
            }
            
            .container {
                -webkit-overflow-scrolling: touch; /* Smooth scrolling on iOS */
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="user-greeting">
                <strong>👋 Welcome, <%= user.username %>!</strong>
                <% if (user.role === 'admin') { %>
                <br><a href="/admin/dashboard" class="admin-link">🛠️ Admin Dashboard</a>
                <% } %>
            </div>
            <a href="/user/logout">
                <button type="button" class="logout-btn">🚪 Logout</button>
            </a>
        </div>
        
        <h1>☁️ GDrive File Storage</h1>
        <p class="welcome-text">Upload and manage your files securely in the cloud with our beautiful interface</p>
        
        <% if (typeof success !== 'undefined' && success) { %>
        <div class="success">
            <strong>✅ Success:</strong> <%= success %>
        </div>
        <% } %>
        
        <% if (typeof error !== 'undefined' && error) { %>
        <div class="error">
            <strong>⚠️ Error:</strong> <%= error %>
        </div>
        <% } %>
        
        <div class="upload-section">
            <div class="upload-icon">📤</div>
            <form action="/user/upload" method="POST" enctype="multipart/form-data" onsubmit="return validateFile()">
                <div class="form-group">
                    <label for="file">🗂️ Choose File to Upload</label>
                    <input type="file" id="file" name="file" required 
                           accept=".jpg,.jpeg,.png,.gif,.webp,.bmp,.tiff,.svg,.mp4,.avi,.mov,.wmv,.flv,.webm,.mkv,.3gp,.mp3,.wav,.aac,.ogg,.flac,.m4a,.pdf,.doc,.docx,.xls,.xlsx,.ppt,.pptx,.txt,.rtf,.zip,.rar,.7z,.tar,.gz,.js,.css,.html,.json,.xml,.csv,.epub,.mobi,.psd,.ai,.sketch">
                    <div class="file-hint">
                        📋 Supported: Images (JPG, PNG, GIF), Videos (MP4, AVI, MOV), Audio (MP3, WAV), Documents (PDF, DOC, PPT, XLS), Archives (ZIP, RAR), Code files & more! (Max: 50MB)
                    </div>
                </div>
                
                <button type="submit">� Upload File</button>
            </form>
        </div>
        
        <script>
        function validateFile() {
            const fileInput = document.getElementById('file');
            const file = fileInput.files[0];
            
            if (!file) {
                alert('Please select a file to upload!');
                return false;
            }
            
            // Check file size (50MB limit)
            const maxSize = 50 * 1024 * 1024; // 50MB
            if (file.size > maxSize) {
                alert('File size must be less than 50MB!');
                return false;
            }
            
            return true;
        }

        // Enhanced file input with drag and drop
        document.addEventListener('DOMContentLoaded', function() {
            const fileInput = document.getElementById('file');
            const container = document.querySelector('.container');
            
            // File input change event
            fileInput.addEventListener('change', function(e) {
                const file = e.target.files[0];
                if (file) {
                    const label = document.querySelector('label[for="file"]');
                    label.innerHTML = `📁 Selected: ${file.name}`;
                    label.style.color = '#4caf50';
                }
            });

            // Add drag and drop functionality
            const uploadSection = document.querySelector('.upload-section');
            
            ['dragenter', 'dragover', 'dragleave', 'drop'].forEach(eventName => {
                uploadSection.addEventListener(eventName, preventDefaults, false);
            });

            function preventDefaults(e) {
                e.preventDefault();
                e.stopPropagation();
            }

            ['dragenter', 'dragover'].forEach(eventName => {
                uploadSection.addEventListener(eventName, highlight, false);
            });

            ['dragleave', 'drop'].forEach(eventName => {
                uploadSection.addEventListener(eventName, unhighlight, false);
            });

            function highlight(e) {
                uploadSection.style.background = 'linear-gradient(135deg, #bbdefb 0%, #90caf9 100%)';
                uploadSection.style.borderColor = '#1976d2';
                uploadSection.style.transform = 'scale(1.02)';
            }

            function unhighlight(e) {
                uploadSection.style.background = 'linear-gradient(135deg, #e3f2fd 0%, #bbdefb 100%)';
                uploadSection.style.borderColor = '#2196f3';
                uploadSection.style.transform = 'scale(1)';
            }

            uploadSection.addEventListener('drop', handleDrop, false);

            function handleDrop(e) {
                const dt = e.dataTransfer;
                const files = dt.files;

                if (files.length > 0) {
                    fileInput.files = files;
                    const event = new Event('change', { bubbles: true });
                    fileInput.dispatchEvent(event);
                }
            }

            // Add loading animation on page load
            container.style.opacity = '0';
            container.style.transform = 'translateY(20px)';
            
            setTimeout(() => {
                container.style.transition = 'all 0.6s ease';
                container.style.opacity = '1';
                container.style.transform = 'translateY(0)';
            }, 100);

            // Enhanced form submission
            const form = document.querySelector('form');
            form.addEventListener('submit', function(e) {
                const submitBtn = this.querySelector('button[type="submit"]');
                const originalText = submitBtn.textContent;
                
                submitBtn.disabled = true;
                submitBtn.textContent = '⏳ Uploading...';
                submitBtn.style.opacity = '0.7';
                
                // Re-enable after reasonable time
                setTimeout(() => {
                    submitBtn.disabled = false;
                    submitBtn.textContent = originalText;
                    submitBtn.style.opacity = '1';
                }, 10000);
            });
        });
        </script>
        
        <div class="file-section">
            <h2>📁 Your Files <span class="file-count"><%= user.files ? user.files.length : 0 %></span></h2>
            
            <% if (user.files && user.files.length > 0) { %>
                <% user.files.forEach(function(file) { %>
                <div class="file-item">
                    <div class="file-info">
                        <div class="file-name">
                            <% 
                            let displayName = '';
                            let fileExtension = '';
                            let fileIcon = '';
                            
                            if (typeof file === 'string') {
                                // Old format - show the file ID, but try to make it more readable
                                displayName = file.split('/').pop() || file;
                                fileExtension = '';
                                fileIcon = '📄';
                            } else if (file.originalName) {
                                displayName = file.originalName;
                                fileExtension = file.originalName.split('.').pop().toLowerCase();
                            } else if (file.filename) {
                                // If filename contains path, get just the filename part
                                displayName = file.filename.split('/').pop() || file.filename;
                                fileExtension = displayName.split('.').pop().toLowerCase();
                            } else {
                                displayName = 'Uploaded file';
                                fileExtension = '';
                            }
                            
                            // Set appropriate icon based on file extension
                            if (['jpg', 'jpeg', 'png', 'gif', 'webp', 'bmp', 'tiff', 'svg'].includes(fileExtension)) {
                                fileIcon = '🖼️';
                            } else if (['mp4', 'avi', 'mov', 'wmv', 'flv', 'webm', 'mkv', '3gp'].includes(fileExtension)) {
                                fileIcon = '🎬';
                            } else if (['mp3', 'wav', 'aac', 'ogg', 'flac', 'm4a'].includes(fileExtension)) {
                                fileIcon = '🎵';
                            } else if (['pdf'].includes(fileExtension)) {
                                fileIcon = '📄';
                            } else if (['doc', 'docx'].includes(fileExtension)) {
                                fileIcon = '📝';
                            } else if (['xls', 'xlsx'].includes(fileExtension)) {
                                fileIcon = '📊';
                            } else if (['ppt', 'pptx'].includes(fileExtension)) {
                                fileIcon = '📽️';
                            } else if (['zip', 'rar', '7z', 'tar', 'gz'].includes(fileExtension)) {
                                fileIcon = '🗜️';
                            } else if (['txt', 'rtf'].includes(fileExtension)) {
                                fileIcon = '📃';
                            } else if (['js', 'css', 'html', 'json', 'xml'].includes(fileExtension)) {
                                fileIcon = '💻';
                            } else {
                                fileIcon = '📄';
                            }
                            %>
                            <%= fileIcon %> <%= displayName %>
                        </div>
                        <% if (file.uploadDate || (typeof file === 'object' && file.uploadDate)) { %>
                        <div class="file-date">
                            📅 Uploaded: <%= new Date(file.uploadDate).toLocaleDateString() %>
                        </div>
                        <% } %>
                    </div>
                    <div class="file-actions">
                        <% 
                        let fileUrl = '';
                        let fileId = '';
                        let fileName = '';
                        
                        if (typeof file === 'string') {
                            // Old format - file is just a string (public_id)
                            fileUrl = `https://res.cloudinary.com/${cloudinaryCloudName}/raw/upload/${file}`;
                            fileId = file;
                            fileName = file;
                        } else if (file.cloudinaryUrl) {
                            // New format - file has cloudinaryUrl
                            fileUrl = file.cloudinaryUrl;
                            fileId = file.filename || file.publicId || file.originalName;
                            fileName = file.originalName || file.filename;
                        } else if (file.filename) {
                            // New format without cloudinaryUrl - construct URL based on file type
                            const extension = file.originalName ? file.originalName.split('.').pop().toLowerCase() : '';
                            const resourceType = ['jpg', 'jpeg', 'png', 'gif', 'webp', 'bmp', 'tiff', 'svg'].includes(extension) ? 'image' : 
                                               (['mp4', 'avi', 'mov', 'wmv', 'flv', 'webm', 'mkv', '3gp'].includes(extension) ? 'video' : 'raw');
                            fileUrl = `https://res.cloudinary.com/${cloudinaryCloudName}/${resourceType}/upload/${file.filename}`;
                            fileId = file.filename;
                            fileName = file.originalName || file.filename;
                        } else if (file.publicId) {
                            // Use publicId - determine resource type from original name
                            const extension = file.originalName ? file.originalName.split('.').pop().toLowerCase() : '';
                            const resourceType = ['jpg', 'jpeg', 'png', 'gif', 'webp', 'bmp', 'tiff', 'svg'].includes(extension) ? 'image' : 
                                               (['mp4', 'avi', 'mov', 'wmv', 'flv', 'webm', 'mkv', '3gp'].includes(extension) ? 'video' : 'raw');
                            fileUrl = `https://res.cloudinary.com/${cloudinaryCloudName}/${resourceType}/upload/${file.publicId}`;
                            fileId = file.publicId;
                            fileName = file.originalName || file.publicId;
                        }
                        
                        // Ensure we always have a fileUrl and fileId
                        if (!fileUrl && fileId) {
                            fileUrl = `https://res.cloudinary.com/${cloudinaryCloudName}/raw/upload/${fileId}`;
                        }
                        %>
                        <a href="<%= fileUrl || '#' %>" 
                           target="_blank" class="view-btn"
                           <% if (!fileUrl) { %>onclick="alert('File URL not available'); return false;"<% } %>>
                            👁️ View
                        </a>
                        <form action="/user/delete-file" method="POST" style="display: inline;">
                            <input type="hidden" name="filename" value="<%= fileId %>">
                            <button type="submit" class="delete-btn" onclick="return confirm('⚠️ Are you sure you want to delete this file? This action cannot be undone!')">
                                🗑️ Delete
                            </button>
                        </form>
                    </div>
                </div>
                <% }); %>
            <% } else { %>
                <div class="no-files">
                    <div class="no-files-icon">📂</div>
                    <strong>No files uploaded yet</strong><br>
                    <small style="color: #999; margin-top: 10px; display: block;">
                        ✨ Upload your first file using the beautiful form above!
                    </small>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>